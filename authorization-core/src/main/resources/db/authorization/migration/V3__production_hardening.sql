INSERT INTO AUTH_SYNC_STATE (SYNC_KEY, STATUS, UPDATED_AT, DETAILS, VERSION)
VALUES ('GLOBAL_SEED_INITIALIZATION', 'READY', CURRENT_TIMESTAMP, NULL, 0);

CREATE TABLE AUTH_CACHE_INVALIDATION (
  ID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  EVENT_ID VARCHAR(100) NOT NULL UNIQUE,
  ORIGIN VARCHAR(100) NOT NULL,
  SCOPE VARCHAR(30) NOT NULL,
  EXTERNAL_ISSUER VARCHAR(500),
  EXTERNAL_SUBJECT VARCHAR(500),
  CREATED_AT TIMESTAMP WITH TIME ZONE NOT NULL
);

CREATE INDEX IX_AUTH_CACHE_INVALIDATION_CREATED
  ON AUTH_CACHE_INVALIDATION(CREATED_AT);
