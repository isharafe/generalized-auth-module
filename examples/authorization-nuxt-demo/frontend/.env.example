# Private Nitro-to-Spring origin.
AUTHORIZATION_BACKEND_URL=http://localhost:8082

# Browser-visible Nuxt origin. Nitro converts this into trusted forwarded headers
# so Spring generates OAuth2 and OIDC logout redirects for port 3000.
NUXT_PUBLIC_BASE_URL=http://localhost:3000
