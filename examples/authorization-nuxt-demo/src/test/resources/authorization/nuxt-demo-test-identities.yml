authorization:
  seed:
    users:
      - issuer: local
        subject: viewer
        username: viewer
      - issuer: local
        subject: manager
        username: manager
      - issuer: local
        subject: admin-user
        username: admin-user
    user-assignments:
      - issuer: local
        subject: viewer
        target-type: ROLE
        target-code: HR_VIEWER
        source: SEED
      - issuer: local
        subject: manager
        target-type: ROLE
        target-code: HR_MANAGER
        source: SEED
      - issuer: local
        subject: admin-user
        target-type: ROLE
        target-code: DEMO_AUTHORIZATION_ADMIN
        source: SEED
