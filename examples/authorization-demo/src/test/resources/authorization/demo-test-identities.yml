authorization:
  seed:
    users:
      - issuer: local
        subject: viewer
        username: viewer
      - issuer: local
        subject: manager
        username: manager
    user-assignments:
      - issuer: local
        subject: viewer
        target-type: ROLE
        target-code: HR_VIEWER
        source: SEED
      - issuer: local
        subject: manager
        target-type: ROLE
        target-code: HR_MANAGER
        source: SEED
      - issuer: local
        subject: manager
        target-type: ROLE
        target-code: AUTHZ_SYSTEM_ADMIN
